from services.observability.audit_event.models import AuditEventEntry


class AuditEventRepository:
    """Placeholder repository for future User_*Operation audit mapping."""

    def list_events(self) -> list[AuditEventEntry]:
        return []

